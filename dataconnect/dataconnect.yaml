specVersion: "v1"
serviceId: "fitbody"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fitbody-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
